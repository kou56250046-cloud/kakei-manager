# 家計管理アプリ 仕様書

**バージョン**: 1.0
**作成日**: 2026-08-03
**運用コスト**: 完全無料（月額 0 円 / 従量課金なし）

### 確定済みの前提

| 項目 | 決定内容 |
|---|---|
| **閲覧環境** | PC + **スマホからも閲覧する** → Firebase 構成を採用 |
| **献金の計算基準** | **手取り（振込額）の 1/10** |
| **既存アプリ** | 特定の指定なし → **新規構築**とし、既存データがあれば後から取り込む |
| **固定費** | 家賃・管理費 / 電気・ガス・水道 / 携帯・ネット回線 / 保険・サブスク の**4分類すべて**を管理 |
| **口座** | **銀行口座を2つ以上**管理 → 口座間振替・口座別残高突合に対応 |
| **初期データ** | **直近3ヶ月分**のカード明細を遡って取り込む |

> 残るは各固定費の金額・引落口座など**実データの入力のみ**（実装時のセットアップで確定）。

---

## 1. 背景と目的

### 1.1 現状の課題

| 課題 | 内容 |
|---|---|
| **入力が続かない** | レシートを1件ずつ手入力する運用が破綻している |
| **全体像が見えない** | 口座残高・収入・支出が一箇所にまとまっていない |
| **献金額の計算が手作業** | 収入の 1/10 を毎回電卓で計算している |

### 1.2 本システムのゴール

> **「毎月10分の作業で、家計の全体像が更新される」状態をつくる。**

そのために、入力作業を次の3種類に分解し、それぞれ最適な方法をとる。

| 種類 | 例 | 入力方法 | 頻度 |
|---|---|---|---|
| **変動費（カード）** | 食費・日用品・通販 | **CSV取り込み（自動）** | 月1回 |
| **固定費** | 家賃・光熱費・通信費 | **マスタ登録 → 毎月自動生成** | 初回のみ |
| **収入・現金支出** | 給与・現金払い | **手入力（数件のみ）** | 月1回 |

**設計上の最重要原則：**
> レシートは1枚も入力しない。カード利用分はCSVが代替する。
> 手入力は「給与額」「口座残高」「現金支出（あれば）」の3つだけに限定する。

---

## 2. 設計方針

### 2.1 「続く」ための3原則

1. **入力ではなく確認にする** — システムが分類まで済ませ、人は「合っているか」を見るだけ
2. **判断が必要なものだけ人に回す** — 自動分類できなかった数件のみ「要確認キュー」に上げる
3. **月次でまとめて処理する** — 日々の記帳を前提にしない

### 2.2 完全無料を成立させる方針

- **外部の有料APIを一切使わない**（Claude API の従量課金も発生させない）
- **AIによる処理は Claude Code（VSCodeチャット）上で対話的に実行**する。バッチでAPIを叩かない
- **クレジットカード・銀行のアカウントアグリゲーション連携を行わない**（有料サービスのため）
- データベースは**ファイル（JSON）＋Git管理**を基本とし、サーバー費用をゼロにする

> **補足：Supabase 無料枠を採用しない理由**
> Supabase 無料プランは **7日間アクセスがないとプロジェクトが自動停止**する。
> 本システムは「月1回しか触らない」運用のため、毎回停止解除が必要になり運用が破綻する。
> **Firestore にはこの自動停止がない**ため、月次運用のアプリに適している。

---

## 3. システム構成（確定）

スマホからも閲覧するため、**Firebase 構成**を採用する。

### 3.1 全体像 ─ 「Git が正、Firestore は閲覧用ミラー」

```
┌─ PC（書き込み側）─────────────────────────┐
│                                                    │
│  imports/meisai202608.csv （Shift_JIS）            │
│         ↓  Claude Code に「取り込んで」            │
│  scripts/import-card.js                            │
│         ↓  パース・分類・重複除去・検算            │
│  data/*.json  ← ★データの正（Git で履歴管理）      │
│         ↓  npm run sync                            │
│  scripts/sync-firestore.js（Admin SDK・一方向）    │
└────────────────────┬───────────────────────┘
                     ↓
              ┌── Firestore ──┐   ← 閲覧用ミラー
              └──────┬────────┘
                     ↓ 読み取りのみ（要ログイン）
┌─ スマホ / PC ブラウザ（閲覧側）────────────┐
│  Firebase Hosting 上の Next.js（静的書き出し）     │
│  Firebase Auth（Googleログイン・自分のみ許可）     │
│  ホーム画面に追加すればアプリのように使える（PWA） │
└────────────────────────────────────────┘
```

**この構成にする理由：**

- **重複判定・検算・分類はローカルで完結する**ため、Firestore への書き込み競合が起きない
- **Git 履歴がそのままバックアップ**になる（Firestore が壊れても復元できる）
- スマホ側は**読むだけ**なので、セキュリティルールが単純になる（誤操作でデータが壊れない）

### 3.2 採用技術と無料枠

| 役割 | 採用 | 無料枠 | 課金リスク |
|---|---|---|---|
| データの正 | JSON + Git（GitHubプライベート） | 無制限 | なし |
| 閲覧用DB | **Cloud Firestore** | 読取5万/日・書込2万/日・1GB | 実質到達不能 |
| ホスティング | **Firebase Hosting** | 10GB/月 転送・360MB/日 | 実質到達不能 |
| 認証 | **Firebase Authentication** | Googleログインは無料 | なし |
| 処理 | Node.js（ローカル実行） | ─ | なし |
| AI処理 | Claude Code（VSCode） | 既存契約内 | なし |

> **月1人しか使わない**ため、無料枠の1%も消費しない。
> Firebase は「Sparkプラン（無料）」のままにしておけば、**上限を超えると課金ではなく停止**になる。
> クレジットカードを登録しない限り、想定外の請求は構造的に発生しない。

**採用しなかった選択肢：**

| 選択肢 | 不採用の理由 |
|---|---|
| Supabase | 7日間アクセスがないと自動停止（月次運用と相性が悪い） |
| Vercel | 認証機能がないため、家計情報の保護に別途仕組みが必要 |
| GitHub Pages | プライベートリポジトリの公開には有料プランが必要 |

### 3.3 リポジトリ構成

```
家計管理リポジトリ（GitHub プライベートリポジトリ）
│
├── data/                        ← ★データの正（JSON）
│   ├── transactions/2026-06.json    取引明細（月別）
│   ├── fixed_costs.json             固定費マスタ
│   ├── incomes.json                 収入記録
│   ├── accounts.json                口座マスタ
│   ├── balances.json                口座残高スナップショット
│   ├── tithe.json                   献金記録
│   └── category_rules.json          自動分類ルール
│
├── imports/                     ← カードCSVを置く場所（.gitignore）
│   └── meisai202608.csv
│
├── scripts/
│   ├── import-card.js               CSV → transactions 変換
│   ├── generate-fixed.js            固定費の当月分を生成
│   ├── calc-summary.js              月次集計・献金計算
│   └── sync-firestore.js            data/ → Firestore へ一方向同期
│
├── app/                         ← Next.js（静的書き出し → Firebase Hosting）
├── .env.local                   ← Firebase Admin の鍵（.gitignore）
└── CLAUDE.md                    ← Claude Code への作業指示書
```

### 3.4 役割分担

| 操作 | 場所 |
|---|---|
| CSV取り込み・分類確定・給与入力・残高入力 | **PC（Claude Code）** |
| 残高・収支・カテゴリ別支出・献金状況の閲覧 | **スマホ / PC ブラウザ** |

> スマホからのカテゴリ編集は将来拡張とする（双方向同期が必要になり、複雑さが跳ね上がるため）。

---

## 4. データモデル

### 4.1 取引（Transaction）

すべての支出・収入を統一形式で保持する。

```json
{
  "id": "20260616-tepco-3656-0",
  "date": "2026-06-16",
  "billing_month": "2026-08",
  "amount": 3656,
  "type": "expense",
  "category": "水道光熱費",
  "subcategory": "電気",
  "merchant_raw": "東京電力　電気料金等",
  "merchant": "東京電力",
  "source": "card",
  "account_id": "aeon_gold",
  "payment_method": "1回払い",
  "is_fixed_cost": true,
  "fixed_cost_id": "fc_denki",
  "note": "６月分",
  "confidence": "high",
  "needs_review": false
}
```

| フィールド | 説明 |
|---|---|
| `id` | 重複防止用の一意キー（4.2 参照） |
| `date` | **利用日**。分析はこの日付を基準にする |
| `billing_month` | カード請求月。口座からの引落予測に使う |
| `type` | `expense` / `income` / `transfer` / `tithe` |
| `source` | `card` / `fixed` / `manual` / `bank` |
| `is_fixed_cost` | 固定費として登録済みの支払いか（二重計上防止） |
| `confidence` | 自動分類の確度 `high` / `medium` / `low` |
| `needs_review` | 人の確認が必要か。ダッシュボードの「要確認キュー」に出る |

### 4.2 重複防止（冪等性）の担保

同じCSVを2回読み込んでも重複しないよう、以下で `id` を生成する。

```
id = {利用日YYYYMMDD}-{店名の正規化キー}-{金額}-{同一グループ内の連番}
```

- 同日・同店・同額の取引が複数あり得るため（例：セリア 110円が別日に2件）、末尾に**グループ内連番**を付与する
- 取り込み時、既存 `id` と一致するレコードは**スキップ**する
- 取り込み結果は必ず `新規N件 / スキップM件` を表示する

### 4.3 固定費マスタ（FixedCost）

```json
{
  "id": "fc_rent",
  "name": "家賃",
  "amount": 85000,
  "category": "住居費",
  "payment_method": "bank_transfer",
  "account_id": "mizuho_main",
  "payment_day": 27,
  "start_month": "2026-04",
  "end_month": null,
  "amount_type": "fixed",
  "match_pattern": null
}
```

**`account_id` には引落口座を指定する。** 口座が複数あるため、
「家賃はメイン口座、保険はサブ口座」のように**固定費ごとに引落元が異なるケース**に対応する。

| `payment_method` | 挙動 |
|---|---|
| `bank_transfer`（口座振替） | **毎月自動で取引を生成する** |
| `cash`（現金） | 毎月自動で取引を生成する |
| `card`（カード払い） | **生成しない。CSV取込時に該当行へ固定費タグを付ける**（← 二重計上を防ぐ） |

| `amount_type` | 説明 |
|---|---|
| `fixed` | 毎月同額（家賃・サブスク） |
| `variable` | 月ごとに変動（電気・ガス・水道）→ 予算額として `amount` を持ち、実額はCSV or 手入力で上書き |

#### 登録する固定費（確定した4分類）

| 分類 | 想定される項目 | `amount_type` | 想定 `payment_method` |
|---|---|---|---|
| 住居費 | 家賃 / 管理費 / 駐車場 | `fixed` | 口座振替 |
| 水道光熱費 | 電気 / ガス / 水道 | `variable` | **電気はカード払い（CSV実績あり）** / ガス・水道は要確認 |
| 通信費 | 携帯 / インターネット回線 | `fixed`（概ね） | 要確認 |
| 保険・サブスク | 生命保険 / 医療保険 / Amazonプライム 等 | `fixed` | **Amazonプライムはカード払い（CSV実績あり）** |

> 各項目の**金額・引落日・引落口座・支払方法**は、実装時のセットアップで1回だけ登録する。
> 以降は一切触らない（金額改定時のみ編集）。

> **⚠️ 重要な設計判断**
> 添付CSVには `東京電力 電気料金等 3,656円` と `Amazonプライム会費 600円` が含まれている。
> これらを固定費マスタからも自動生成すると**二重計上**になる。
> → カード払いの固定費は「生成せず、CSVの行に `is_fixed_cost: true` を付ける」方式とする。

### 4.4 口座（Account）

銀行口座を2つ以上管理するため、口座に**役割（role）**を持たせる。

```json
[
  {
    "id": "mizuho_main",
    "name": "みずほ銀行（メイン）",
    "type": "bank",
    "roles": ["salary_deposit", "card_payment", "fixed_cost_payment"],
    "display_order": 1,
    "include_in_total": true
  },
  {
    "id": "bank_sub",
    "name": "◯◯銀行（サブ）",
    "type": "bank",
    "roles": ["savings"],
    "display_order": 2,
    "include_in_total": true
  },
  {
    "id": "aeon_gold",
    "name": "イオンゴールドカード",
    "type": "credit_card",
    "settlement_account_id": "mizuho_main",
    "closing_day": 10,
    "payment_day": 2,
    "include_in_total": false
  }
]
```

| `type` | 説明 |
|---|---|
| `bank` | 銀行口座。残高突合の対象 |
| `credit_card` | カード。残高ではなく**未払い残高（次回請求額）**を持つ |
| `cash` | 現金 |
| `emoney` | PASMO等（今回はスコープ外。将来拡張） |

| `roles` | 意味 |
|---|---|
| `salary_deposit` | 給与の振込先 |
| `card_payment` | カード代金の引落口座 |
| `fixed_cost_payment` | 固定費の引落口座（固定費ごとに個別指定も可） |
| `savings` | 貯蓄用。生活費と分けて表示する |

> **カード口座の `settlement_account_id`** により、「イオンカードの請求は、みずほから8/3に引き落とされる」
> という**キャッシュフロー予測が自動で組み立てられる**。

### 4.5 口座間振替（Transfer）

口座が2つ以上あると「メイン口座 → 貯蓄口座へ移動」が発生する。
**これを支出として計上すると家計が二重に目減りする**ため、専用の型で扱う。

```json
{
  "id": "trf_20260725_001",
  "date": "2026-07-25",
  "type": "transfer",
  "amount": 50000,
  "from_account_id": "mizuho_main",
  "to_account_id": "bank_sub",
  "note": "貯蓄へ移動"
}
```

| ルール | 内容 |
|---|---|
| 集計 | **収入にも支出にも計上しない**。カテゴリ別集計から完全に除外する |
| 残高 | from の残高を減らし、to の残高を増やす |
| 献金 | **献金の対象収入に含めない**（同じお金の移動のため） |
| 手数料 | 振込手数料が発生した場合は、別途 `expense`（手数料）として計上 |

> **カード代金の引き落としも内部的には transfer 相当**だが、
> 支出は既に利用日ベースで計上済みのため、`card_settlement` として区別し、
> **残高だけを動かして支出には計上しない**。（二重計上の防止）

### 4.6 残高スナップショット（Balance）

口座ごとに月1回記録する。

```json
[
  {
    "account_id": "mizuho_main",
    "date": "2026-08-01",
    "actual_balance": 432150,
    "calculated_balance": 435600,
    "diff": -3450,
    "diff_reason": "現金支出の記録漏れ"
  },
  {
    "account_id": "bank_sub",
    "date": "2026-08-01",
    "actual_balance": 850000,
    "calculated_balance": 850000,
    "diff": 0
  }
]
```

- `actual_balance` は**通帳／ネットバンキングを見て手入力**する（月1回・口座ごと）
- **貯蓄口座（`savings`）は差額がほぼ出ない**ため、確認は数十秒で終わる
- `calculated_balance` はシステムが取引から計算した理論値
- 差額が出たら「不明支出」として自動で調整取引を作るか、原因を記録する

### 4.7 収入（Income）

> 献金は**手取り基準**のため、`net_amount`（実際の振込額）が必須項目。
> `gross_amount` は任意（記録用・年次サマリー用）。

```json
{
  "id": "inc_202607_salary",
  "date": "2026-07-25",
  "name": "給与",
  "gross_amount": 380000,
  "net_amount": 298000,
  "deposit_account_id": "mizuho_main",
  "is_tithe_target": true,
  "tithe_base": "net"
}
```

### 4.8 献金（Tithe）

```json
{
  "month": "2026-07",
  "base_amount": 380000,
  "rate": 0.1,
  "calculated_amount": 38000,
  "paid_amount": 38000,
  "paid_date": "2026-07-28",
  "carry_over": 0
}
```

---

## 5. カード明細CSV 取り込み仕様

添付ファイル `meisai202608.csv`（イオンゴールドカード）の**実データで検証済み**の仕様。

### 5.1 ファイル形式

| 項目 | 値 |
|---|---|
| 文字コード | **Shift_JIS（CP932）** ※ UTF-8 ではないので変換必須 |
| 改行コード | CRLF |
| 列数 | 9列（後半は空列） |
| 拡張子 | `.csv` |

### 5.2 ファイル構造

```
行0  ご利用カード, イオンゴールド            ← カード名
行1  今回ご請求金額, 181534                  ← 請求総額（検算に使用）
行2  お支払い日, 2026年 8月 3日              ← 口座引落日
行3  ご指定口座
行4  金融機関, 支店, 口座番号, 名義人         ← ★取り込まない（個人情報）
行5  ﾊﾝｶﾒｲ, ｼﾃﾝﾒｲ, ｺｳｻﾞﾊﾞﾝｺﾞｳ, ﾒｲｷﾞﾆﾝ      ← ★取り込まない（個人情報。実値はここに伏せた）
行6  ご利用明細
行7  ご利用日,利用者区分,ご利用先,支払方法,,,ご利用金額,備考   ← 明細ヘッダ
行8〜 明細データ（79件）
行87 分割・ボーナス払い明細                  ← ★ここで明細ブロック終了
行88 ご利用日,ご利用先,支払回数,...          ← 分割払いブロック（今回は0件）
```

### 5.3 パース手順

1. Shift_JIS で読み込み、UTF-8 に変換
2. `今回ご請求金額` を取得 → 検算用に保持
3. `お支払い日` を取得 → `billing_month` と引落予定日を決定
4. `ご利用明細` の次の**ヘッダ行の次**から読み始める
5. `分割・ボーナス払い明細` 行に到達したら**明細ブロック終了**
6. 各行を以下でマッピング

| 列 | 項目 | 変換ルール |
|---|---|---|
| 0 | ご利用日 | `260616` → `2026-06-16`（先頭2桁 + 2000 が西暦年） |
| 1 | 利用者区分 | `本人` / `家族` → `card_holder` に格納 |
| 2 | ご利用先 | `merchant_raw` に原文保持。全角スペース・全角英数を正規化して `merchant` を生成 |
| 3 | 支払方法 | `１回` → `1回払い` |
| 6 | ご利用金額 | 整数化 |
| 7 | 備考 | `ポイント２倍対象` 等。`note` に格納 |

7. **検算**：明細の合計金額が `今回ご請求金額` と一致することを確認
   - 検証結果：79件・合計 **181,534円** = 請求額 **181,534円** ✅
   - **不一致の場合は取り込みを中止し、エラーを表示する**
8. 分割・ボーナス払いブロックに行があれば、別テーブルとして取り込む（今回は0件）

### 5.4 店名の正規化

CSVの店名は全角・半角カナ・伸ばし棒の揺れが激しいため、正規化してから分類する。

| 処理 | 例 |
|---|---|
| 全角英数 → 半角 | `ＡＭＡＺＯＮ．ＣＯ．ＪＰ` → `AMAZON.CO.JP` |
| 全角スペース（U+3000）除去 | `クリエイト　エス　デイ－` → `クリエイトエスデイ－` |
| 全角ハイフン類の統一 | `デイ－` `モバイルパスモチヤ－` → `ー` に統一 |
| 前後空白の除去 | ` ６月分` → `６月分` |

### 5.5 ⚠️ 利用日と請求月のズレ（重要）

添付CSVの利用日は **2026/05/28 〜 2026/07/10** に分布している（請求は 2026/08/03）。

- イオンカードは **毎月10日締め・翌月2日払い**のため、1つのCSVに**2ヶ月分の利用日**が混在する
- さらに `260528` `260531` のように、**2ヶ月以上前の利用が遅れて計上される**ことがある

**設計上の対応：**

| 用途 | 使う日付 |
|---|---|
| 月次の支出分析・カテゴリ集計 | **利用日（`date`）** |
| 口座残高の引落予測・キャッシュフロー | **請求月・支払日（`billing_month`）** |

> **過去月の集計は後から変わり得る。** 集計値をファイルに固定保存せず、
> 表示時に取引データから毎回再計算する設計にする（`calc-summary.js`）。

### 5.6 個人情報の除去

CSVには**金融機関名・支店名・口座番号・名義人**が含まれる。
取り込み時に**これらの行は一切保存しない**。`imports/` に置いた元CSVは取り込み後に削除するか、`.gitignore` に登録する。

```gitignore
imports/*.csv
```

---

## 6. カテゴリ自動分類

### 6.1 分類方式

**ルールベース（正規表現）を第一段階、Claude Code による判断を第二段階**とする。AI APIの従量課金は発生しない。

```
CSV1行
  ↓
① category_rules.json のパターンマッチ  → 一致すれば confidence: high
  ↓ 一致しない
② 過去の同一店名の分類履歴を参照        → 一致すれば confidence: high
  ↓ 該当なし
③ needs_review: true として「要確認キュー」へ
  ↓
④ Claude Code に「未分類を分類して」と依頼 → 提案を人が承認 → ルールに自動追加
```

> **④で承認したルールは `category_rules.json` に自動追記される。**
> 使うほど未分類が減り、数ヶ月で要確認は月0〜3件程度に収束する。

### 6.2 カテゴリ体系（案）

| 大分類 | 中分類 |
|---|---|
| 食費 | スーパー / コンビニ / 外食 / 宅配水 |
| 日用品 | ドラッグストア / 100円ショップ / ホームセンター |
| 住居費 | 家賃 / 管理費 |
| 水道光熱費 | 電気 / ガス / 水道 |
| 通信費 | 携帯 / インターネット |
| 交通費 | 交通系ICチャージ / ガソリン / 自転車 |
| 医療・健康 | 医療機関 / 薬 / 眼鏡 |
| 被服費 | 衣類 / 靴 |
| 教養・娯楽 | 書籍 / サブスク / レジャー |
| 旅行 | 宿泊 / 交通 |
| 通販（要内訳） | Amazon 等 |
| **献金** | 什一献金 / その他献金 |
| 特別費 | 冠婚葬祭 / 家電 |
| 不明 | 要確認 |

### 6.3 初期分類ルール（添付CSVから作成）

```json
[
  { "pattern": "まいばすけっと|いなげや", "category": "食費", "subcategory": "スーパー" },
  { "pattern": "キッチンオリジン", "category": "食費", "subcategory": "外食" },
  { "pattern": "クリクラ", "category": "食費", "subcategory": "宅配水" },
  { "pattern": "ウェルパーク|クリエイトエスデイ", "category": "日用品", "subcategory": "ドラッグストア" },
  { "pattern": "セリア|ダイソ|ジエ.ソン", "category": "日用品", "subcategory": "100円ショップ" },
  { "pattern": "ドンキホーテ", "category": "日用品", "subcategory": "雑貨" },
  { "pattern": "東京電力", "category": "水道光熱費", "subcategory": "電気", "is_fixed_cost": true },
  { "pattern": "ゼンノウ|ヒガシニホンエネル", "category": "交通費", "subcategory": "ガソリン" },
  { "pattern": "モバイルパスモ", "category": "交通費", "subcategory": "ICチャージ" },
  { "pattern": "ジテンシヤ", "category": "交通費", "subcategory": "自転車" },
  { "pattern": "ユニクロ|しまむら|ワークマン", "category": "被服費", "subcategory": "衣類" },
  { "pattern": "Zoff", "category": "医療・健康", "subcategory": "眼鏡" },
  { "pattern": "バリユ.ブツクス|バリューブックス", "category": "教養・娯楽", "subcategory": "書籍" },
  { "pattern": "Amazonプライム会費", "category": "教養・娯楽", "subcategory": "サブスク", "is_fixed_cost": true },
  { "pattern": "EXPEDIA|イクスピアリ", "category": "旅行", "confidence": "medium" },
  { "pattern": "AMAZON.CO.JP", "category": "通販（要内訳）", "needs_review": true },
  { "pattern": "ラインイーシー", "category": "通販（要内訳）", "needs_review": true }
]
```

### 6.4 添付CSVを分類した結果（試算）

| 店舗 | 金額 | 分類 | 判定 |
|---|---:|---|---|
| AMAZON.CO.JP | 29,445 | 通販（要内訳） | ⚠️ **要確認** |
| EXPEDIA | 29,238 | 旅行 | △ 中確度 |
| イクスピアリ：催事ワゴン２ | 25,200 | 旅行/娯楽 | ⚠️ **要確認** |
| クリエイトエスデイ | 17,919 | 日用品 | ✅ |
| ビーンズ登戸 Zoff | 9,900 | 医療・健康 | ✅ |
| 全農東日本エネル | 9,636 | 交通費（ガソリン） | △ 要確認 |
| その他 | 60,196 | 各種 | ✅ |

> **示唆：最大支出の Amazon（29,445円 / 全体の16%）が用途不明のままになる。**
> これは分類ロジックでは解けないため、**Amazon の注文履歴だけは月1回確認する運用**を推奨する。
> （Amazonの購入履歴はCSVダウンロード可能。将来的にこれも取り込み対象にできる）

---

## 7. 献金の自動計算（手取り基準）

### 7.1 「手取り」の定義

本システムにおける手取り＝**給与明細の「差引支給額」＝実際に口座へ振り込まれた金額**とする。

| 項目 | 手取りに含むか |
|---|---|
| 社会保険料（健康保険・厚生年金・雇用保険） | 控除済み（＝含まない） |
| 所得税・住民税 | 控除済み（＝含まない） |
| 財形貯蓄・社内預金・持株会など**自分の資産として積み立てられる控除** | ⚠️ 判断が必要（下記） |
| 社宅費・組合費など**支出性の控除** | 控除済み（＝含まない） |

> **⚠️ 論点：財形貯蓄などの「天引き積立」の扱い**
> これらは手取りから引かれるが、**自分の資産として残る**ため、
> 「実質的には収入」と考えて献金対象に含めるという整理もあり得ます。
> 設定 `add_back_savings_deduction`（true/false）で切り替えられるようにします。
> 該当する天引きがなければ気にしなくて構いません。

### 7.2 計算式

```
献金対象額 = Σ( net_amount )  ※is_tithe_target = true の収入
           + ( add_back_savings_deduction が true なら 天引き積立額 )

献金額 = 端数処理( 献金対象額 × 0.10 )
```

### 7.3 設定項目（`config.json`）

```json
{
  "tithe": {
    "rate": 0.1,
    "base": "net",
    "rounding": "ceil",
    "rounding_unit": 100,
    "include_bonus": true,
    "include_side_income": true,
    "add_back_savings_deduction": false,
    "carry_over_enabled": true
  }
}
```

| 設定 | 既定値 | 説明 |
|---|---|---|
| `base` | **`net`** | 手取り（振込額）基準 ← 確定 |
| `rate` | `0.1` | 献金率 |
| `rounding` / `rounding_unit` | `ceil` / `100` | 100円単位で切り上げ（設定変更可） |
| `include_bonus` | `true` | 賞与も手取り額で対象に含める |
| `include_side_income` | `true` | 副収入も対象に含める |
| `add_back_savings_deduction` | `false` | 天引き積立を対象に戻すか（7.1参照） |
| `carry_over_enabled` | `true` | 未献金分を翌月に繰り越すか |

**計算例（手取り 298,000 円の場合）**

```
298,000 × 0.10 = 29,800 円
→ 100円単位で切り上げ → 29,800 円
```

### 7.4 副収入がある場合の扱い

副収入（業務委託・受託など）は**経費と源泉徴収を差し引いた実入金額**を `net_amount` とする。
給与と同じテーブルに登録すれば、自動的に月の献金対象額へ合算される。

### 7.5 繰越（未献金残高）の管理

```
未献金残高 = Σ(計算された献金額) − Σ(実際に献金した額)
```

- 月次で自動計算し、ダッシュボードに常時表示
- 収入が複数回ある月（給与＋賞与＋副収入）も自動で合算対象になる
- 献金の実行記録は `tithe.json` に手入力（月1回）
- 献金そのものも**支出取引として計上**され、口座残高に反映される

### 7.6 表示イメージ

```
━━━━━━━━━━━━━━━━━━━━━━━
  💠 献金（2026年7月）
━━━━━━━━━━━━━━━━━━━━━━━
  手取り収入     298,000 円
   ├ 給与        298,000 円
   └ 副収入            0 円
  献金率              10%
  ────────────────────────
  計算額          29,800 円
  献金済          29,800 円
  ────────────────────────
  今月残               0 円  ✅
  累積未献金           0 円
━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 8. 銀行口座・残高管理

カード連携を行わないため、以下の方式で残高を管理する。

### 8.1 残高の考え方

```
理論残高 = 前回スナップショット残高
         + その後の収入
         − その後の口座振替支出（固定費）
         − その後のカード引落
         − 手入力した現金支出
```

### 8.2 月次の突合（リコンサイル）

1. 月初にネットバンキングで**実際の残高**を口座ごとに確認して入力（2口座なら2回）
2. システムが**理論残高**を計算して並べて表示
3. 差額があれば以下を選択
   - 「不明支出として記録する」→ 調整取引を自動生成
   - 「原因を入力する」→ メモを残す

> この仕組みにより、**現金支出をすべて記録しなくても残高が狂わない**。
> 記録漏れは「不明支出」として月次でまとめて可視化される。

### 8.3 複数口座の表示方針

| 表示 | 内容 |
|---|---|
| 合計残高 | `include_in_total: true` の口座の合計（＝一番大きく表示する数字） |
| 口座別残高 | 口座ごとの残高と前月比 |
| 生活費 / 貯蓄の分離 | `savings` ロールの口座は別枠で表示し、**「使えるお金」と「貯めたお金」を混ぜない** |

> 2口座あることで「メインは減っているが、貯蓄に移しただけ」という状況が正しく見える。
> これは振替（4.5）を支出から除外しているからこそ成立する。

### 8.4 キャッシュフロー予測

カード請求は利用の**約1〜2ヶ月後**に引き落とされるため、先の残高を予測できる。

```
【みずほ銀行（メイン）】
2026-08-03  イオンゴールド引落   −181,534 円（確定・CSVより自動）
2026-08-25  給与                +〇〇〇,〇〇〇 円（収入）
2026-08-27  家賃                −〇〇,〇〇〇 円（固定費）
2026-08-27  携帯・ネット        −〇,〇〇〇 円（固定費）
────────────────────────────────
2026-08-31  予測残高             〇〇〇,〇〇〇 円

【◯◯銀行（サブ・貯蓄）】
2026-08-31  予測残高             〇〇〇,〇〇〇 円
```

> **カードの請求額はCSVの `今回ご請求金額` から自動で取得される**ため、
> 引落予定は手入力なしで確定値として反映される。

---

## 9. 画面仕様

### 9.1 ダッシュボード（トップ）

| ブロック | 内容 |
|---|---|
| 口座残高サマリー | 各口座の残高と合計、前月比 |
| 今月の収支 | 収入 / 支出 / 差引 |
| カテゴリ別支出 | 円グラフ or 横棒グラフ（金額大きい順） |
| 献金ステータス | 計算額 / 献金済 / 未献金残高 |
| **要確認キュー** | 未分類・要レビュー取引の件数と一覧（← ここが日々の作業の入口） |
| 支払予定 | カード引落予定・固定費の引落日 |

### 9.2 取引一覧

- 期間 / カテゴリ / 口座 / 金額でフィルタ
- カテゴリのインライン編集（変更時「今後同じ店名も同じ分類にする？」→ ルール自動追加）
- 検索（店名）

### 9.3 固定費マスタ

- 一覧・追加・編集・停止（`end_month` 設定）
- 変動固定費（電気・ガス）は予算額と実績額を並べて表示

### 9.4 収入・献金

- 収入の登録（給与・賞与・副収入）
- 献金額の自動計算表示と、献金実績の記録
- 年間の献金推移

### 9.5 月次レポート

- カテゴリ別の前月比・前年同月比
- 支出推移グラフ（6ヶ月）
- 固定費と変動費の比率

---

## 10. 運用フロー

### 10.1 初回セットアップ（1回だけ・約60分）

```
【S1】口座マスタの登録                        （10分）
     銀行2口座＋イオンゴールドカードを登録
     引落口座・給与振込口座の紐付けを設定

【S2】固定費マスタの登録                      （20分）
     家賃 / 電気・ガス・水道 / 携帯・ネット / 保険・サブスク
     各項目の 金額・引落日・引落口座・支払方法 を入力
     ※カード払いのものは payment_method: card にする（二重計上防止）

【S3】直近3ヶ月分のカード明細CSVを取り込み      （15分）
     イオンカード会員サイトから3ヶ月分をダウンロード
     imports/ に3ファイルまとめて配置
     → Claude Code に「imports の明細を全部取り込んで」

【S4】カテゴリの一括確定                      （15分）
     ★金額の大きい順に上から確定していく（下記の運用ルール参照）

【S5】起点となる口座残高を入力                （5分）
     3ヶ月前時点の各口座残高を入力 → ここから残高が積み上がる
```

#### ⚠️ 3ヶ月分をまとめて取り込む際の注意

| 論点 | 対応 |
|---|---|
| **利用日が重複する** | 各CSVは締め日の関係で2ヶ月分の利用日にまたがる（例：8月請求分に5/28の利用がある）。3ファイル間で同じ取引が重複し得るため、**`id` による重複除去が必須**（4.2参照） |
| **検算は請求月ごとに行う** | 3ファイルを結合してから検算すると成立しない。**ファイル単位で「明細合計＝今回ご請求金額」を検証**する |
| **未分類が一気に200件超出る** | 下記のパレート運用で処理する |

#### 未分類を捌く運用ルール（初回を挫折させないために）

> **金額の大きい順に並べ、上位から確定していく。**
> 添付CSVの実績では、**上位6店舗で全体の6割（約12万円/18万円）**を占めている。
>
> 1. **上位20件を確定する**（→ 金額ベースで8割以上がカバーされる）
> 2. **残りの少額はカテゴリ「その他」で一括確定してよい**
> 3. 確定したルールは `category_rules.json` に自動蓄積され、**2ヶ月目以降の未分類は月0〜3件に収束する**
>
> 完璧に分類しようとしないこと。**家計の傾向を掴むのに1円単位の精度は不要**。

---

### 10.2 月次運用（想定所要時間：10〜12分）

```
【1】カード明細CSVをダウンロード             （2分）
     イオンカード会員サイト → imports/ に配置

【2】Claude Code に依頼                        （1分）
     「imports の明細を取り込んで」
     → Shift_JIS変換・パース・分類・重複除去・検算 まで自動
     → 結果表示：「新規79件 / スキップ0件 / 検算OK / 要確認3件」

【3】要確認キューを処理                        （3分）
     Amazon等の数件だけカテゴリを確定
     → ルールが自動学習され、翌月以降は減る

【4】給与を入力                                （1分）
     → 献金額が自動計算される

【5】口座残高を入力                            （3分）
     銀行2口座の残高をネットバンキングで確認して入力
     → 各口座で理論値との差額をチェック
     → 口座間で移動があれば「振替」として登録（支出に計上されない）

【6】同期・デプロイ                            （1分）
     「同期してコミットして」
     → npm run sync でFirestoreへ反映
     → git commit で履歴保存
     → スマホから最新の状態が見られる
```

**固定費は一切触らない。** マスタ登録済みのものが毎月自動生成される。
**カード引落額も手入力不要。** CSVの `今回ご請求金額` から自動で残高に反映される。

---

## 11. Claude Code への指示書（CLAUDE.md）

リポジトリ直下に `CLAUDE.md` を置き、以下を明記する。

- CSVの文字コードは必ず Shift_JIS で読むこと
- 取り込み前に必ず既存 `id` と照合し、重複を登録しないこと
- 取り込み後に必ず「明細合計 = 今回ご請求金額」を検算し、不一致なら中止すること
- 口座番号・名義人の行は**絶対に保存しないこと**
- 分類が確定したら `category_rules.json` に追記すること
- 金額の計算は必ず整数（円）で行い、浮動小数点を使わないこと

---

## 12. セキュリティ・プライバシー

| 項目 | 方針 |
|---|---|
| リポジトリ | **必ずプライベート**（GitHub は無料でプライベート可） |
| 元CSV | `.gitignore` に登録。取り込み後に削除 |
| 口座番号・名義人 | 取り込み対象外。保存しない |
| 公開ホスティング | **GitHub Pages は使わない**（プライベートリポジトリの公開には有料プランが必要） |
| 静的ファイル | 家計データを**ビルド成果物に埋め込まない**。必ずログイン後に Firestore から取得する |
| Firebase Auth | 自分のGoogleアカウントの **UID をホワイトリスト化**し、それ以外はログインさせない |
| Firestore ルール | **認証済みの自分のUIDのみ読み取り可・書き込みは全面禁止**（書き込みは Admin SDK 経由のみ） |
| Admin SDK の鍵 | `.env.local` に置き `.gitignore` に登録。**絶対にコミットしない** |
| Firebase の課金 | **Sparkプラン（無料）のまま**にし、クレジットカードを登録しない。上限超過時は課金ではなく停止する |
| バックアップ | Git履歴がそのままバックアップになる |

---

## 13. コスト試算

| サービス | 用途 | 想定使用量 | 無料枠 | 月額 |
|---|---|---|---|---|
| GitHub（プライベート） | コード・データ管理 | 数MB | 無制限 | **0円** |
| Firebase Hosting | スマホ閲覧 | 〜50MB/月 | 10GB/月 | **0円** |
| Cloud Firestore | 閲覧用データ | 読取〜3,000/月 | 読取5万/**日** | **0円** |
| Firebase Authentication | Googleログイン | 1ユーザー | 無料 | **0円** |
| Node.js / Next.js | 処理・ビルド | ローカル実行 | ─ | **0円** |
| Claude Code（VSCode） | CSV取り込み・分類 | 月1回 | 既存契約内 | **0円** |
| 外部AI API | **使用しない** | ─ | ─ | **0円** |
| **合計** | | | | **0円** |

> **無料枠の使用率は1%未満**（Firestore の読取上限は1日5万回、こちらは月3,000回程度）。
> さらに Spark プランのままカードを登録しなければ、**上限超過時も課金ではなく停止**となるため、
> 想定外の請求は構造的に発生しない。

---

## 14. 開発フェーズ

| Phase | 内容 | 目安 | 完了条件 |
|---|---|---|---|
| **P0** | 口座マスタ・固定費マスタのセットアップ | 1時間 | 2口座＋4分類の固定費が登録済み |
| **P1** | データモデル定義 + CSV取り込みスクリプト | 1日 | **3ヶ月分**が重複なく・検算OKで取り込める |
| **P2** | カテゴリ自動分類 + 要確認キュー | 1日 | 8割以上が自動分類される |
| **P3** | 固定費マスタ + 自動生成（二重計上防止込み） | 半日 | 固定費が毎月自動反映される |
| **P4** | 収入・献金計算 | 半日 | 給与入力で献金額が出る |
| **P5** | 複数口座の残高・突合・口座間振替 | 1日 | 口座別残高と差分が表示され、振替が支出計上されない |
| **P6** | ダッシュボードUI（Next.js） | 2日 | 全体像が1画面で見える |
| **P7** | Firebase 認証 + Firestore同期 + Hosting デプロイ | 1日 | **スマホから見られる** |
| **P8** | 月次レポート・推移グラフ | 1日 | 前月比が見られる |

> **P1〜P5 が完成した時点でPCから実用可能。P7 でスマホ閲覧が開通する。**
> まず P1〜P4（取り込み・分類・固定費・献金）を最優先とする。

---

## 15. 将来の拡張候補（今回はスコープ外）

- Amazon 注文履歴CSVの取り込み（通販の内訳を自動化）
- 複数カード対応（家族カード・別ブランド）
- 予算設定とアラート（カテゴリ別の使いすぎ通知）
- 年間サマリー / 確定申告用の集計出力
- 分割払い・ボーナス払いの残債管理（CSVに該当ブロックあり）

---

## 16. 要確認事項

### 16.1 確定済み

| # | 項目 | 決定内容 |
|---|---|---|
| Q1 | 既存アプリの構成 | 指定なし → **新規構築** |
| Q2 | 閲覧環境 | **スマホからも閲覧** → Firebase構成を採用 |
| Q3 | 献金の計算基準 | **手取り（振込額）の 1/10** |
| Q4 | 固定費の分類 | **家賃・管理費 / 電気・ガス・水道 / 携帯・ネット / 保険・サブスク** の4分類すべて |
| Q5 | 口座構成 | **銀行口座2つ以上** → 口座間振替・口座別突合に対応 |
| Q6 | 過去データ | **直近3ヶ月分**を初回に取り込む |

### 16.2 実装時のセットアップで入力する項目（仕様には影響しない）

> 以下は**仕様の決定事項ではなく、登録する実データ**です。
> 実装のP0（セットアップ）で1回だけ入力すれば完了します。

| 項目 | 内容 |
|---|---|
| 銀行口座 | 各口座の名称・役割（給与振込 / カード引落 / 貯蓄） |
| 固定費の明細 | 各項目の金額・引落日・引落口座・支払方法（口座振替 / カード / 現金） |
| 給与 | 支給日・支給回数・賞与の有無 |
| 起点残高 | 3ヶ月前時点の各口座残高 |

### 16.3 実装前に一度だけ確認したい論点

| # | 項目 | 影響 |
|---|---|---|
| A | **天引き積立（財形貯蓄・持株会等）の有無** | ある場合、献金対象に含めるかの判断が必要（7.1参照）。**なければ設定不要** |
| B | ガス・水道・携帯・保険が**カード払いかどうか** | カード払いなら固定費マスタから自動生成せず、CSV側にタグを付ける（二重計上防止）。セットアップ時に判明すればよい |
| C | カードは1枚のみか（家族カード・他ブランド） | 複数ある場合、CSVフォーマットが異なるためパーサーの追加が必要 |

---

*本書は実装の進行に応じて随時更新する。*
*バージョン管理：Git（GitHub プライベートリポジトリ）*
