# このファイルは正典ではない

**作業指示はリポジトリ直下の `CLAUDE.md` を読むこと。** 仕様の正は `家計管理アプリ_仕様書.md`。

このファイルには以前、直下の `CLAUDE.md` の**古い写し**が置かれていた。
内容が二重管理になった結果、次の2点が実装と真逆のまま「絶対に守ること」として残り、
**唯一のバックアップを消す指示**になっていた。

| 古い記述 | 実際 |
|---|---|
| データの正は `data/*.json`（**Git管理**） | `.gitignore` に `data/` がある。**Git 管理外** |
| **GitHub Pages は使わない** | 使っている。`docs/index.html` に全データを AES-256-GCM で暗号化して公開している |

この2点を素直に信じると「`data/` は Git にあるから壊れても戻せる」と誤認し、
さらに「Pages を使わない制約に違反している」と判断して `docs/` を消す。
**`docs/index.html` は生成物であると同時に、`data/` の唯一のバックアップ**である。
`npm run restore` で合言葉だけから `data/` 一式を戻せるのは、このファイルがあるから。

## ここに書いてよいもの

直下の `CLAUDE.md` に**書かれていないこと**だけ。同じ内容を繰り返さない。
今のところ該当なし。

> 実データ（金額・店名・件数）をこのファイルに書かないこと。
> `ui/` ほどではないが、リポジトリは public である。
