# 家計管理アプリ — Claude Code への作業指示書

`家計管理アプリ_仕様書.md` が仕様の正。本ファイルは**実装上の約束事**を書く。

## この構成の要点

- **ローカル完結**。Firebase・Next.js・外部DB・外部AI APIは使わない。**依存パッケージはゼロ**
- **データの正は `data/*.json`**。ただし**Git管理しない**（`.gitignore` 済み）
- `dist/index.html` は生成物であり、いつ消してもよい
- GitHub は **public リポジトリ**。無料プランで Pages を使うには public である必要がある
  （`docs/index.html` は AES-256-GCM で暗号化しているため、公開しても中身は読めない）

### バックアップは `docs/index.html`

`data/` をコミットしていないため、**リポジトリのファイル一覧はバックアップにならない**。
代わりに `docs/index.html` に全データを暗号化して同梱しており、これが実質のバックアップ。
`npm run restore` で `data/` 一式を戻せる（必要なのは合言葉だけ）。

**そのため、`data/` に新しいファイルを足したら `build-web.js` の `backup` にも追加すること。**
入れ忘れるとそのファイルだけ復元できなくなる（実際 `category_rules.json` と
`recurring_incomes.json` が抜けており、後から追加した）。

## 絶対に守ること

1. **CSVは必ず Shift_JIS で読む**。`new TextDecoder('shift_jis')` を使う（`cp932` というラベルは Node が受け付けない）
2. **口座番号・名義人（CSV 行4-5）は絶対に保存しない**。`scripts/lib/parse-meisai.js` が読み捨てている。ここを変更しない
3. **取り込み前に必ず既存 `id` と照合し、重複を登録しない**
4. **検算は必ず行う。ただし不一致でも中止しない** — 警告して `data/import_log.json` に記録し、続行する
   - 仕様書11章は「不一致なら中止」としているが、**返品行（マイナス金額）やポイント充当で実際にズレ得る**ため、中止すると運用が止まる
5. **金額は必ず整数（円）で扱う。浮動小数点を使わない**
6. **分類が確定したら `data/category_rules.json` に追記する**
7. `imports/*.csv`・`給与明細/*.pdf`・`dist/` は `.gitignore` 済み。**コミットしない**

## コマンド

```bash
npm run close            # ★月次の締め。対話で分類と残高を確定し、以下を全部通す
npm run close -- --commit  # git commit まで
npm run close -- --yes     # 対話なし

npm run import           # imports/*.csv → data/transactions/YYYY-MM.json
npm run import:payslip   # 給与明細/*.pdf → data/incomes.json
npm run generate         # 固定費マスタ・定期収入マスタ → 月次の取引・収入を生成
npm run summary          # 集計をターミナルに表示（npm run summary -- 2026-05 で月指定）
npm run build            # data/ + ui/ → dist/index.html（単一ファイル）
npm run update           # import → import:payslip → generate → build を一括実行

npm run review -- --list # 要確認の一覧（正規化キー付き）
npm run review -- --all  # category_rules.json を直した後、既存データに反映する

# 特定の1件だけ分類を確定する（商業施設など、店名ルールにできないもの）
node scripts/set-category.js --match ノクテイプラザ --date 2026-05-02 \
     --category 教養・娯楽 --sub 書籍 --note "ベビー用の布絵本"
```

### 店名ルールにするか、1件だけ確定するかの判断

| 店の性質 | 対応 | 例 |
|---|---|---|
| 扱う物が決まっている | `category_rules.json` にルールを追加 | スーパー・ドラッグストア・書店 |
| **毎回中身が違う** | **`set-category.js` で1件だけ確定** | 商業施設・百貨店・モール |

後者にルールを作ると翌月以降を誤分類する。`set-category.js` が付ける `manual_override: true` は
`npm run review -- --all` でも上書きされない。

## 月次運用（10分）

1. イオンカード会員サイトから明細CSVをダウンロードし `imports/` に置く
2. 給与明細PDFを `給与明細/` に置く
3. `npm run update`
4. 要確認が出たらルール追記 → `npm run review -- --all`
5. 月初にネットバンキングで残高を確認し `data/balances.json` に追記 → `npm run build`
6. `dist/index.html` を開いて確認 → `git commit`

**固定費は触らない。カード引落額も手入力しない**（CSVの「今回ご請求金額」から自動取得）。

## 店名の正規化（`scripts/lib/normalize.js`）

分類用の `merchant_key` と表示用の `merchant` を分けている。

1. `normalize('NFKC')` — 全角英数→半角、半角カナ→全角カナ
2. 空白（U+3000 含む）をすべて除去
3. **カタカナ直後の**ハイフン類のみ長音「ー」に統一

手順3を「カナ直後のみ」に限定するのが重要。無条件に変換すると `2CO.COM|CYBER-LIAM…` のような英字のハイフンまで壊れる。

> この規則で `クリエイト　エス　デイ－` と `クリエイトエスデイ－` が同一キーに統合される。

## 分類ルールの書き方（`data/category_rules.json`）

- パターンは **`merchant_key`（正規化済み）に対して**大小文字を無視してマッチする
- **配列の上から順に評価し、先にヒットしたものが勝つ**
  - 例：`Amazonプライム会費` は `AMAZON.CO.JP` より**前**に置く必要がある
- 正規化後の表記に注意する。例：`くまざわ書店` はひらがな（`クマザワ` では当たらない）
- カード払いの固定費には `is_fixed_cost: true` を付ける

## 固定費・定期収入の自動生成（`scripts/generate-fixed.js`）

### ★二重計上を防ぐ分岐（最重要）

生成するかどうかは `auto_generate` で明示する。判断基準は「**そのカードの明細CSVを取り込んでいるか**」。

| 状況 | `auto_generate` | 理由 |
|---|---|---|
| 口座振替・現金 | **true** | 明細に出てこないので生成しないと漏れる |
| **イオンゴールド**払い | **false** | CSVに実額が出る。生成すると**二重計上** |
| **dカード**払い（ドコモ） | **true** | **CSVを取り込んでいないので、生成しないと支出から完全に漏れる** |

仕様書4.3は「カード払いは生成しない」としているが、これは**明細を取り込んでいるカードに限った話**。
取り込んでいないカードまで生成しないと、その支出は永久に集計されない。

### 生成物の扱い

`source: 'fixed'` / `'recurring'` を持ち、**実行のたびに全部消して作り直す**。
元データではなく導出データなので、マスタを直したら必ず `npm run generate` を再実行する。

### 十分の一献金

`amount_type: "computed"` / `compute: "tithe"` で、金額を固定せず
**その月の献金対象収入 × `config.json` の `tithe.rate`（100円単位で切り上げ）**を毎月計算する。

献金は**支出取引としても計上される**（仕様書7.5）。そのため「献金済」の額は
tithe.json ではなく**什一献金の支出取引から数える**（`ui/summary.js` の `titheByMonth`）。
別管理にすると「支出には出ているのに未献金と表示される」矛盾が起きる。
tithe.json に記録がある月はそちらを正として上書きする。

## 集計期間の下限（`config.json` の `analysis.start_month`）

**2025-12 より前の利用日は取り込まない。** 理由：

- 最古のCSV（2026-01請求分）の利用日が **2025-10-30 始まり**のため、2025-11 は上旬が構造的に欠ける
- 2025-10 は2件しかない
- 給与明細も 2025-12 支給分からしかなく、収支が成立しない
- 2025-12 は3ファイルで全日カバーされている（＝ここが最初の完全な月）

**ファイルを消すだけでは再取込で復活する**ので、必ず `analysis.start_month` で制御する。
`import-card.js` が取込時に除外し、範囲外の月別ファイルも削除する。
**検算は明細の全行で行っているため、この除外は検算結果に影響しない。**

### 「不完全な月」の判定

`import_log.json` の `usage_from` / `usage_to` からカバー範囲を求め、
**月の全日がカバー範囲に収まっているか**で判定する（`markIncompleteMonths`）。

単純に「最初と最後の月＝不完全」としてはいけない。`analysis.start_month` で意図的に
切った場合、その月は「範囲外」ではなく完全な月なので、不完全と表示すると誤りになる。

## ★ `ui/` のコードは公開HTMLに埋め込まれる

`ui/summary.js` と `ui/app.js` は、`docs/index.html`（GitHub Pages で公開）に
**ソースのまま**入る。**コメントに実際の金額・店名・口座名を書かないこと。**

実際に「住居費64,330が…」という説明コメントを書いて公開HTMLに漏らしかけた。
ビルド時の平文混入チェック（`build-web.js`）が検出して止めたが、
チェックが無ければそのまま公開されていた。

`data/` に新しいファイルを足したときは `build-web.js` の `backup` にも追加する
（入れ忘れるとそのファイルだけ `npm run restore` で復元できない）。

## ツールチップは `bindTip` を通す（`ui/app.js`）

グラフに詳細を出す箇所は**必ず `bindTip` を使う**。`onmousemove` / `onmouseleave` を直に書かない。

**タッチでは `mouseleave` が発火しない。** ブラウザはタップのたびに `mouseenter`/`mousemove` を
エミュレートして送ってくるが、離れたことを知らせる `mouseleave` は他の場所を叩くまで来ない。
そのためスマホで**詳細が画面に残り続けていた**（本人の指摘で判明）。

`bindTip` が touch と mouse の両方を1箇所で見る。指を置いている間だけ出し、離す・スクロールに
移る（`touchcancel`）と消える。

- **端末で分岐しない。** `ontouchstart in window` などで二分すると、タッチ対応ノートPCで必ず壊れる。
  「直前の入力が指だったか」（`isTouching()`）だけで判定する
- **エミュレートされたマウスイベントは `touchend` の「後」に飛んでくる。**
  そのため `touchend` で消すだけでは直後の `mousemove` がもう一度出してしまう。
  直近にタッチがあった間はマウス側を丸ごと無視している

## 画面はタブで分かれている（`ui/app.js` の `initTabs`）

カードが17枚まで増え、1枚の縦長ページでは端まで届かなくなったため、5タブに分けた。

| タブ | 中身 |
|---|---|
| 今月 | 月次推移／いつもの幅／カテゴリ別／日別カレンダー |
| 傾向 | 変動費と固定費の推移／光熱費／店舗別 |
| 固定費 | 固定費の変化／年間コミット額／固定費と変動費／固定費一覧 |
| お金の出入り | 口座残高／カード引落予定／献金 |
| 明細 | 要確認キュー／取引一覧 |

### カードを足すとき

`data-tab="..."` を付け、**同じタブのカードは隣り合わせに置く**（`display:none` で隠すため、
順序が混ざると grid の行が崩れる）。`span-*` は**そのタブの中で**成立する組み合わせにする。

### 守ること

1. **警告（`#banners`）と期間フィルタはタブに入れない。** 残高不足のような行動に繋がるものは、
   どのタブを見ていても目に入る必要がある
2. **描画は全タブ分やり、表示だけ切り替える。** 開いた瞬間に組み立てると切替のたびに待たされる
3. **隠れたタブに溜まるものはタブに件数を出す**（`updateTabBadges`）。
   タブ導入で唯一失うのが「全部が目に入る」ことなので、そこだけ補う
4. **印刷では全カードを出す**（`@media print` で `.card.is-hidden` を戻している）

## 分析カードの落とし穴（実データで踏んだもの）

数式が通ることと正しい答えを出すことは別。以下は実際に誤った出力を出したもの。

| 関数 | 踏んだ罠 | 対処 |
|---|---|---|
| `priceChanges` | 請求のない月（合計0円）を「値上がり前」として誤報 | 0円の月と不完全な月を比較から除外 |
| `annualCommitment` | 「実績月数÷対象月数」で毎月の費目を隔月と判定し、年額が半分になった | **請求間隔の中央値**で判定（末尾の欠落に強い） |
| `categoryBands` | 支出が無かった月を幅に入れず、0円の月が「過去最低」になった | 取引の無い月も0円として幅に含める |
| `dailyTotals` | 固定費が月初計上のため、カレンダーの初日だけが極端に濃くなった | 既定で固定費を除外（トグルで表示可） |

**ドル建てのサブスクは為替で毎月わずかに動く。** これを値上がりとして拾うと毎月鳴り、
本当の改定が埋もれる。`priceChanges` は最小差額と最小変化率で足切りし、
さらに「2ヶ月以上続いたか」（`settled`）で確定と様子見を分けている。

## PWA（ホーム画面に置く／オフラインで開く）

`npm run build:web` が `docs/` に以下を生成する。**すべて生成物なので手で編集しない。**

| ファイル | 中身 |
|---|---|
| `manifest.webmanifest` | アプリ名・アイコン・`display: standalone` |
| `sw.js` | Service Worker。ビルド時刻をキャッシュ名に埋め込む |
| `icon-192.png` / `icon-512.png` / `apple-touch-icon.png` | `scripts/lib/icon.js` がコードから描く |

### 守ること

1. **パスは必ず相対（`./`）にする。** GitHub Pages はリポジトリ名のサブディレクトリ
   （`/kakei-manager/`）で配信されるため、`/` 始まりだとドメイン直下を指してインストールもキャッシュも動かない
2. **Service Worker は「ネットワーク優先」にする。** 家計データは `index.html` に同梱されており
   毎月まるごと書き換わる。キャッシュ優先にすると**更新したのに先月の数字が出る**という、
   このアプリで最悪の事故になる。通信が無いときだけキャッシュに落とす
3. **キャッシュ名を毎ビルド変える**（`VERSION` にビルド時刻）。これで `activate` 時に
   古い世代＝先月のデータを抱えたHTMLを確実に捨てられる
4. `manifest` と `sw.js` は `index.html` に同梱できない（Service Worker は独立ファイル必須）。
   **単一ファイル原則の唯一の例外**がこの2つ＋アイコン

### アイコンを依存パッケージなしで作る

依存ゼロの原則を守るため、`scripts/lib/png.js` に最小の PNG エンコーダ（RGBA/zlib のみ）を書いてある。
sharp や canvas は入れない。図柄は**中央80%の内側**に収める（Android の maskable で円形に切られるため）。

**棒は右肩上がりにしない。** 家計で棒が伸び続ける絵は「支出が増え続けている」意味になる。山型にしている。

**キャッシュされるのは暗号文のままのHTML。** 復号結果はメモリ上にしか無いので、
キャッシュから家計データは読めない。

## 集計ロジックは1箇所（`ui/summary.js`）

CLI（`calc-summary.js`）とブラウザの両方から使う。`build.js` が `export ` を落として HTML に埋め込む。
**このファイルには import を書かない**（自己完結させる）。

集計値は**保存しない**。カード明細は利用日と請求月がずれ、過去月の集計は後から変わり得るため、常に取引データから再計算する。

## 判明済みの事実（実データ由来・再調査不要）

| 項目 | 内容 |
|---|---|
| カード | イオンゴールド（10日締め・翌月2日払い）と **dカード**（ドコモ料金。明細CSVは未取込） |
| 口座 | みずほ銀行（メイン・給与振込・カード引落）／ 三井住友銀行（貯蓄） |
| 光熱費 | **電気・ガス・水道すべてイオンカード払い**（東京電力 / プロパンガス / 川崎市水道料金） |
| ガスの供給元 | 2026-02 までは **JA組合プロパン**、2026-03 から **全農東日本エネルギー**（`ゼンノウヒガシニホンエネル`）。同社はガソリンスタンドもやっているため紛らわしいが、**この明細はガス代**。当初 `交通費/ガソリン` に誤分類しており、本人の指摘で修正した |
| 水道 | **隔月請求**。請求のない月があるのは正常（光熱費の折れ線では点を打たず破線でまたぐ） |
| サブスク | Amazonプライム / Apple / Claude / OpenAI / Microsoft / MyEdit もイオンカード払い |
| MyEdit | 明細表記 `2CO.COM¦CYBERLIAMSTERDAM` は 2Checkout 経由。`CYBERLI`(=CyberLink) ＋ 請求地 `AMSTERDAM`。**年額のため次回は2027年5月ごろ** |
| 天引き積立 | **なし**（拠出金は全月0円）→ `add_back_savings_deduction: false` のままでよい |
| 給与 | 支給日は毎月17〜20日ごろ。対象期間は前月1日〜末日 |
| 給与明細の「その他」欄 | **年末調整の過不足税額が入る**。2025-12支給は −26,670（還付）。**この列を読まないと検算が合わない** |
| 2026-03支給 | 欠勤控除減額 −119,037円で手取り157,760円。社会保険料の控除もない（異常値ではない） |
| 子ども | 2026年1月出生。2月申請、**3月から児童手当 15,000円/月**。西松屋などの育児費が発生する |

## カテゴリ体系の変更（仕様書6.2からの追加）

| 追加 | 理由 |
|---|---|
| `交際費`（ギフト） | LINEギフトなど他人へ贈るものが、どのカテゴリにも入らなかった |
| `育児`（ベビー用品） | 2026年1月に出生。西松屋などが継続的に発生する |
| `教養・娯楽 / 資格・学習` | ITパスポート試験・CBTS受験 |
| `特別費 / 奨学金返済` | 毎月の返済 |

## 未確定・要セットアップ

- 各固定費の**引落日**が未確認（`payment_day: null`）。月初で計上している。
  月次集計には影響しないが、残高予測の日付精度が落ちる
- **児童手当を献金の対象に含めるか**は未確定。現在 `is_tithe_target: false`（＝含めない）
